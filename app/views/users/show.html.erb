<h2>Books</h2>
<p><%= link_to "プロフィール編集", edit_user_path(@user.id) %></p>
<table>
  <tbody>
    <% @books.each do |book| %>
     <tr>

      <td><%= book.title %></td>
      <td><%= book.body %></td>

     </tr>
    <% end %>
  </tbody>
</table>
