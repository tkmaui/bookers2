<div>
  <%= flash[:notice] %>
</div>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <tr><%= image_tag @user.get_profile_image(100,100) %></tr>
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <th><%= @user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= @user.introduction %></th>
          </tr>
        </tbody>
      </table>
      <div class="row">
        <%= link_to "プロフィール編集", edit_user_path(@user.id) %>
      </div>




      <h2 class="mt-3">New book</h2>
        <%= form_with model: @book, url:"/books",local:true do |f| %>
        <div class="form-group">
          <div class="field_with_errors">
            <label for="book_title">Title</label>
          </div>
          <div class="field_with_errors">
          <%= f.text_field :title, class:"form-control book_title" %>
          </div>
        </div>
        <div class="form-group">
          <label for="book_opinion">Opinion</label>
          <div class="field_with_errors">
          <%= f.text_area :body, class:"form-control book_body" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.submit 'Creat Book', class:"btn btn-success" %>
        </div>
        <% end %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
        <tbody>
          <tr>
            <td>
              <%= image_tag @user.get_profile_image(100,100) %>
              <br>
              <%= link_to user_path(@user.id) do %>
                <%= @user.name %>
              <% end %>
            </td>
            <td>
              <%= link_to book_path(@book.id) do %>
                <%= @book.title %>
              <% end %>
            </td>
            <td><%= @book.body %></td>
            <td><%= link_to "Edit", edit_book_path(@book.id), class:"btn btn-sm btn-success edit_book_77113" %></td>
            <td><%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？",class:"btn btn-sm btn-danger destroy_book_77113" %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>